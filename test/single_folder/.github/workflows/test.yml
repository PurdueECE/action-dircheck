name: Self Test (Valid)
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Self Test (Valid)
        uses: ./
        with:
          paths: "test/single_folder"